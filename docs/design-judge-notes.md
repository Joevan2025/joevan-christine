JUDGE 0 (an art director who has seen a thousand wedding websites and is allergic to anything that looks like a Squarespace/Zola template or an AI default):
winner=guest-journey; Judged as a wedding site whose actual job is getting ~200 guests, on phones, from a Messenger link, to RSVP and find three venues: guest-journey wins because it is the only plan that treats the phone as the primary surface (thumb bar, 16px inputs, Waze, calendar, swatch card, contextual countdown) while still honouring botanical-editorial with ink lines and misregistered pastel washes, and it is buildable in vanilla HTML/CSS/SVG with a stated byte budget. Its weakness is originality: its vine spine and wash are shared with garden, and its bottom bar and pills flirt with app grammar; mitigate by dropping the sticky audio mini-bar and the third font family. Garden is the most distinctive and the best pure art direction (one ink, vine-as-skeleton, herbarium, ampersand-free but hierarchy-true entourage tree) and would be my pick for a couple whose guests are on desktops; it loses on mobile clarity (no persistent RSVP affordance, gutter stolen at 360px, spelled-out countdown) and on feasibility (the desktop tree and the text-carrying garlands are under-specified exactly where they are hard). Stationery is the most polished-sounding and the most template-adjacent: the opening envelope is the SE-Asian e-invite cliché, foil plus wax seal plus paper grain is the Zola luxe kit, and its signature is a 140vh scroll-hijack that the plan itself expects to disable on target devices. Recommended build: guest-journey's skeleton and mobile systems, with garden's single-ink rule, arch-as-only-shape and herbarium palette sheets (merged with the swatch-card save), and stationery's time-headed venue cards with travel note, rite glyphs, and documented 5:7 gallery slot convention.
  stationery: total 24 | steal: The 5:7 / 7:5 print-proportion system governing every card and the gallery slots (with documented slot orientation: 1,3,6 portrait; 2,4,5 landscape) so the couple's photos drop in without layout breakage; Four culturally specific 16px line glyphs for the secondary-sponsor rites (veil, cord, candle, arrhae) as the only icons on the page; Venue cards headed by TIME not venue name, ordered south-to-north, with the connective travel note between them; Paint-chip fan deck for the palette with hex shown on tap (guests shopping fabric); Countdown as one typeset tabular line, seconds dropped, updating once a minute; Hashtag pun typeset with TIN/JOE upright 600 against italic 300 (all three share this, keep it); Static SVG route card replacing the map iframe, iframe only on click; Deckle edge reserved for the one card a guest 'tears off' (RSVP): a single, meaningful use of texture | weak: The envelope-that-opens hero is the single most overused digital-wedding-invite trope in Southeast Asia (every Indonesian/Filipino 'undangan digital' opens with an envelope and a seal). It reads as the e-invite template, not as expensive stationery; Champagne-gold foil hairline + wax seal + feTurbulence paper grain is the exact Zola/Minted 'luxe' kit; it drags the page toward a generic premium template and the foil sits outside the mandated palette; A 140vh sticky scroll-hijack hero on the device that matters (low-end Android in Messenger's in-app browser) is the riskiest possible first impression; the plan itself admits this and punts it to a data-lite flag; Wax-seal-as-submit is an affordance gamble: a round monogram does not read as 'send' to a tita; it needs a labelled button, at which point the seal is decoration; No persistent RSVP affordance on mobile beyond a nav pill at the unreachable top of the screen; the thumb-zone problem is unaddressed; Effort is the highest of the three (3D hinge with scroll-timeline plus IO fallback, deckle masks, 6 SVG files plus a 6-glyph sprite, five states for the RSVP card) for a signature that may have to be disabled on target devices; Letterpress inset shadows on every card plus paper texture is more 'rendered' than 'printed'
  garden: total 27 | steal: One drawing ink (--stem, a deepened sage) for every botanical so the system reads as one hand; pastels only ever as misregistered washes 3-4px off the line; The vine as structure, not ornament: the same plant becomes the programme stem, the entourage tree and the footer wreath. The 'remove it and the sections lose their skeleton' test is the right test; Entourage hierarchy encoded as branch depth: principal sponsors as the heaviest boughs, parents as roots. Mobile fallback of indent = branch depth is a clean, scannable single column; Herbarium palette: three specimen sheets, one per colour family, with latin name and a real collection line 'coll. Bacolod, xi.2026'; The arch as the only non-rectangular shape on the site, derived from the Redemptorist church niche, recurring in hero/gallery/video/music; Matthew 19:6 set as a textPath ring around the wreath (with plain-text repeat for a11y); Colophon column: families typeset as invitation wording instead of 'The Couple' cards; Opening the gallery image in the same tab instead of a lightbox, because that is what Messenger's browser handles best | weak: Mobile guest clarity is the weakest of the three: no persistent RSVP affordance, a 'Menu' word in the top corner, a hero CTA that says 'Reply by 15 October' rather than an action, and a 24px vine gutter permanently stolen from a 360px column; The spelled-out countdown sentence ('ninety days, four hours and twelve minutes from now') is charming once and slower to parse every time after; a guest wants a number; The desktop entourage tree is under-specified where it is hardest: 'tree SVG drawn behind each tier as a row-level background' does not say how branch strokes connect tiers of different heights and name counts; expect a week of fiddling or a visual compromise; Garland crossings that carry the eyebrow text 'tied on like a label' plus a document-height SVG assembled from 13 segment templates with Y positions rewritten on ResizeObserver is a build-step-sized job for a no-build-step site; No map at all (link only) is defensible but the Venue section then has no visual, and distances are marked 'to be verified'; Spine is scroll-driven via rAF on scroll events rather than animation-timeline with fallback; fine, but the plan underestimates repaint cost of a 10,000-unit-tall SVG on cheap GPUs; Herbarium sheets and arched frames are, as the self-critique admits, Pinterest-garden tropes; the justification is good copy but a guest won't read the justification; Fern = Negros is a lovely note that nobody will perceive; five motifs plus two structural pieces is more drawing than the budget needs
  guest-journey: total 33 | steal: Mobile bottom thumb bar: Schedule / Send our RSVP / Menu, safe-area aware, hides while the RSVP section is in view, flips to 'Replied' after submit via localStorage. This is the one idea that actually moves the RSVP rate; Ampersand-axis entourage: 1fr auto 1fr grid with the '&' as the centre column, pairs pivot on it, sticky group labels so a ninong scrolling ten rows knows where he is; Per-stop Navigate / Waze / Add-to-calendar pills with lat/lon lifted from the existing map links; Waze is the real default in Negros traffic; Swatch card the guest can save and show the dressmaker (canvas PNG to navigator.share), because that is what the palette section is actually for; Countdown second line that changes with time: '...and n days to reply' / 'replies closed, see you in Bacolod' / 'today, 2:00 PM, Queen of Peace'; Dress code as sponsors-vs-guests first, women/men second, with a struck-through white chip; 16px inputs, 48px fields, autocomplete/inputmode attributes, honeypot, buildPayload() as the single phase-2 seam, empty #rsvp-followup slot so copy never promises an email yet; Newsreader (opsz) as the reading face under Cormorant display: two serifs doing display vs text is how real invitations are set; @font-face size-adjust overrides so the swap from system serif doesn't jump; Byte budget stated per file with an FCP target on mid-range Android | weak: The two signature devices (vine spine, misregistered wash) are identical to the garden proposal's; on its own this page's decoration is the least original of the three, and the vine here is a gutter ornament rather than structure; A bottom tab bar and pill buttons are app grammar; combined with a sticky 'Now playing' mini-bar stacked above the nav bar on mobile, the bottom of the screen gets crowded and the page starts to feel like a PWA, not stationery. Drop the sticky audio bar; The hero's big '90' display number is the default countdown with the boxes removed; the contextual second line rescues it but the composition is still the commodity; Instrument Sans is fast becoming the 2025-26 AI default sans; three families is ~110KB of fonts where two would do; Venue 'ticket' rows and the 2x2 (then 2x3) gallery mosaic are, as the self-critique concedes, the default shapes; the route-map sketch and the tall first frame are patches, not rethinks; Palette swatch PNG via canvas and share sheet is nice-to-have scope that can slip; fine as a stretch, should not block launch; Twelve gallery slots is a lot of empty drawn frames if the couple only delivers six photos; needs a count in config that collapses the grid

JUDGE 1 (a front-end engineer who must build this in vanilla HTML/CSS/JS with inline SVG, no build step, in a few days, and cares about performance on cheap Android phones):
winner=guest-journey; Scored as the engineer who has to ship this in vanilla HTML/CSS/SVG in a few days and test it on a 2-3GB Android inside the Messenger in-app browser. Garden is the most beautiful idea on paper and the least buildable: a document-length vine whose anchors drift with every reflow, and a desktop entourage tree that is a diagram masquerading as a reflowable layout; it also leaves the phone with no persistent RSVP control. Stationery has the strongest 'printed object' system (5:7 suite, seal-as-submit, pathLength trick) but spends its performance budget on the wrong things: a fixed feTurbulence texture and a 3D scroll-driven envelope as the first impression, both of which are exactly what stutters on cheap phones, and the envelope gimmick is itself a template trope. Guest-journey wins on the two axes that decide whether 200 guests actually RSVP from a phone at 11pm: the bottom thumb bar, proper input hygiene, Waze/.ics/tel links, parking where people look, and a spine that is cheap (straight path, non-scaling stroke, one custom-property write per frame). Its weaknesses are scope bloat and a less surprising look, both of which are fixable by cutting features and borrowing: take stationery's seal-submit, print-slot gallery and sticky group headers, and garden's drawn route north, stem programme and indented-rank entourage for mobile. Ship guest-journey's skeleton with those grafts and a trimmed feature list (drop the swatch PNG, lightbox swipe and music mini-bar from v1), and fix the four load-bearing bugs all three found (Cormorant Garant, #meal-group, YouTube URL-as-ID, Rikkerink placeholder parents) before any styling.
  stationery: total 27 | steal: pathLength="1" on every botanical path so draw-on CSS is one rule (dasharray 1) with no getTotalLength JS; Wax seal as the RSVP submit with the press + wash-ring success micro-animation and four explicit data-rsvp-status states (idle/sending/sealed/error) that leave room for a phase-2 email state; Static SVG route card with a 'Load interactive map' button instead of shipping the Google iframe in initial HTML; Countdown as one typeset line, seconds dropped, updated once a minute; Fixed 5:7 / 7:5 print slots with documented slot orientation in the filename convention, plus img onerror flipping a data-empty placeholder; Sticky group headers inside the entourage section on mobile; Four 16px line glyphs for veil / cord / candle / coins as the only icons on the page; data-lite flag (saveData / low deviceMemory) that drops the heavy effects separately from prefers-reduced-motion; Hashtag pun typeset with TIN / JOE upright 600 against italic 300; The 'Found in the file' bug list (Cormorant Garant, #meal-group, playlist URL in YOUTUBE_VIDEO_ID, unclosed entourage divs) | weak: body::before feTurbulence paper texture, position: fixed, full page = a filtered compositor layer that repaints on every scroll on cheap Android; on 2-3GB phones this alone makes scrolling stutter, and the data-lite guard relies on navigator.deviceMemory which is Chrome-only; The signature (sticky 140vh hero, 3D clip-path flap, scroll-driven card rise, scroll-driven wisteria draw) is the most expensive thing on the page and it is the first thing a guest sees inside the Messenger in-app browser; the author admits this is untested. animation-timeline: scroll() has ~60% support so the IO+rAF fallback is the real code path; Guest has to scroll 110vh of animation on a phone before reading the actual invitation card; a tita opening the link for the date gets an envelope first; An 'envelope that opens' hero is itself one of the most common wedding-template gimmicks (dozens of Wix/Squarespace/Zola themes do it), so the distinctiveness is in the suite system, not the signature; Adds --foil champagne gold, a colour outside the mandated palette; defensible but it is a new hue to defend to the couple; Asset workload is large for a few-day build: 4 motifs + crest + liner tile + 6 glyphs = 12 hand-drawn SVGs, plus deckle mask SVG, plus liner pattern; Mask-image deckle edges and inset outline-offset hairlines are fiddly across WebKit/Blink and easy to get wrong on the one card that matters (RSVP); Palette chips show hex 'on tap/hover' which means a hidden tap state on touch; guests shopping fabric need the hex visible always; Contact + parking shrink to an 'enclosure strip' under the timeline; parking is one of the top three guest questions and is buried
  garden: total 27 | steal: Venue as a drawn route north (Bacolod → Talisay → Silay) with approximate drive times between stops instead of three cards + iframe; the content's real geometry is the layout; Programme as a true <ol> stem with buds that open when in view, the one honest use of numbering; Entourage on mobile as an indented list where indent = rank (0/16/32/48/64px) with the tier label sitting on the branch line; sponsors typographically heaviest, parents as roots; Herbarium specimen sheets: one plant per colour family, three tints as wash, tappable hex chips that copy; Single drawing ink (--stem) for every botanical so the set reads as one hand; pastels only as misregistered washes; Matthew 19:6 on a <textPath> wreath with a plain <p> duplicate for accessibility; Families listed first on mobile, matching Filipino invitation convention; Success state replaces the card interior with a pressed lavender sprig and 'We've kept a seat for you, {firstName}'; Theme and Parking folded into FAQ where guests actually look; Nav and FAQ deliberately left boring | weak: The full-document vine is the riskiest engineering item of all three: a path spanning documentHeight whose section Y anchors are computed after load and recomputed on ResizeObserver will drift every time lazy images, font swap, or the FAQ <details> open changes layout; the scroll-mapped dashoffset also needs the path length recomputed on every reflow. Budget a full day just for this, more on Android where 100svh and URL-bar resizes fire ResizeObserver constantly; Desktop entourage tree: branch strokes drawn as row-level SVG backgrounds that must visually connect to names in a CSS grid, with flex column-reverse. This is a diagram pretending to be a reflowable layout; with 10 sponsor pairs of uneven name length the branches will not land on the names. Expect to cut it to the mobile list on all sizes; '13 reusable segment templates' for the vine and a hand-drawn arch frame, fern, gown + barong figures, three herbarium plants, wreath: the illustration workload is the largest of the three proposals and it is all blocking (no vine, no page); Mobile nav is 'J & C' + the word 'Menu'; there is no persistent RSVP control on a phone, so the only RSVP entry is the hero CTA or scrolling. For a site whose stated priority is RSVP that is a real miss; Countdown spelled out as words ('ninety days, four hours and twelve minutes') is charming but slower to scan than a number, and the toWords util is code nobody asked for; Gallery taps navigate to the raw JPG in the same tab; inside Messenger's in-app browser that leaves the page and shows a bare image with no back affordance some users will find; Stroke 1.25/1.5px without vector-effect: non-scaling-stroke on a symbol reused at many scales means hairlines vanish or fatten depending on placement; The vine itself is explicitly acknowledged as now-common; remove the tree/stem/wreath (likely the first things cut under schedule pressure) and the page is a draw-on vine over a quiet serif page; No sticky tier labels for the 10-row sponsor list on mobile; No background bands at all plus one ink plus no boxes may read as unfinished to the exact audience (titas on Messenger) rather than expensive; the author flags this but offers no mitigation
  guest-journey: total 30 | steal: Mobile bottom thumb bar (Schedule / Send our RSVP / Menu) with safe-area padding, hidden while the RSVP section is in view, flipping to 'Replied' after submit; RSVP input hygiene: 48px fields, 16px font to stop iOS zoom, autocomplete given-name/family-name/tel/email, inputmode=tel, honeypot, aria-describedby inline errors, a single buildPayload() that owns the JSON contract; Venue stops as 'tickets' with Navigate / Waze / Add to calendar pills; Waze deep link with lat/lon and a client-side .ics data: URL work on GitHub Pages with zero backend; Countdown second line that changes state (days to reply / replies closed / today 2:00 PM); Swatch card the guest can save and show a dressmaker, rendered to PNG and pushed through navigator.share with a new-tab fallback; Dress code as a sponsors-vs-guests × women-vs-men table rather than icon cards; youtube-nocookie facade with a local poster, iframe injected only on tap; @font-face size-adjust overrides on the fallback fonts so the swap does not jump; Explicit byte budget (HTML 45 KB, CSS 18 KB, JS 16 KB, no external images on first paint) and critical CSS inlined; FAQ final item 'How do we reach you?' with tel: and m.me links; parking inside the venue itinerary; The misregistered two-plate wash as the one consistent use of the pastels in artwork; Spine as a straight 40×10000 viewBox path with vector-effect: non-scaling-stroke, driven by a single custom property write per frame | weak: Three Google font families (Cormorant, Newsreader with opsz axis, Instrument Sans) is heavier than either rival's two; ~110 KB is optimistic once the opsz variable axis is included, and Newsreader vs Cormorant at body size is a subtle distinction most guests will never perceive; Feature count is the real schedule risk: bottom bar, bottom-sheet menu, lightbox with swipe, audio player plus a sticky mini-bar, swatch PNG via canvas, .ics builder, Waze links, share API with two fallbacks, map-on-demand, video facade, spine + 7 sprigs, countdown states. Each is small; together they exceed a few days unless the swatch PNG, lightbox swipe and mini-bar are cut first; Two sticky bottom elements on mobile (nav bar + 'Now playing' mini-bar) stack to ~120px on a 640px-tall viewport; the stacking and z-index choreography is not designed; The 2px misregistered wash at 1x DPR on cheap Android is literally a 2-device-pixel smear and will read as blur, not riso; needs a DPR-aware offset or a bigger, looser wash to be legible as intentional; Entourage grid 1fr auto 1fr pivoting on the & at 360px: pairs like 'Mr. Rodolfo Perarin & Mrs. Jenelyn Curameng' wrap each half to 2-3 lines with the & floating mid-air; the plan asserts it works without showing it; localStorage('rsvp:done') is unreliable in Messenger/Facebook in-app browsers, so 'Replied' state will silently reset; Dual scroll-driver (animation-timeline: scroll() behind @supports plus a passive scroll/rAF fallback) is two code paths to test for a spine that is decorative; pick the fallback only; Visually the page (arch over names, spine in the left gutter, hairlines, vellum band) is the least surprising of the three; the distinctiveness is in product decisions, not in the look, so a casual viewer could still file it as 'nice Squarespace'; Gallery asks for 12 prenup photos when the couple has zero; more empty frames than content on launch day; Sprig y-positions computed from offsetTop on load have the same layout-drift problem as the garden vine, just with a smaller blast radius

JUDGE 2 (a Filipino wedding guest in their 50s opening the link from Facebook Messenger on a phone, who needs to RSVP and know where the church is):
winner=guest-journey; Judged as a Filipino guest in their 50s opening the link from Messenger on a mid-range Android: the two things that matter are reaching the RSVP without thinking and seeing where Queen of Peace is with an address and a navigate button. 'guest-journey' is the only plan built around that: a thumb-height Send our RSVP button that is always reachable, a reply card with phone-first fields, a stepper instead of a typed number, a visible street address, Waze plus Google Maps plus calendar per stop, tel: and m.me links, and a Share-to-Messenger action. Its botanical work (spine, sprigs, misregistered wash) is modest but honest to the palette and cheap to build. 'stationery' is the most beautiful on paper but puts a closed envelope between the guest and everything else, stacks scroll-timeline plus 3D hinge plus feTurbulence on the exact WebView it admits it has not tested, adds a gold that is not in the palette, and hides the church address behind a tap. 'garden' is the most original (the tree, the herbarium, the route) but its mobile story is weakest: a word-countdown, no persistent RSVP, a tree that cannot really connect reflowing names, a single green ink that demotes the mandated purple, and a document-height vine that will drift. The right build is guest-journey's skeleton with stationery's pinned RSVP pill and ledger entourage as a fallback if the ampersand axis fails at 360px, garden's route-north venue copy and indent-as-rank mobile entourage, and stationery's secondary-sponsor glyphs.
  stationery: total 25 | steal: Always-visible RSVP pill in the mobile nav (the one thing this persona needs pinned); Wax-seal monogram reused as nav logo, favicon, OG image and submit button: one crest, five jobs; Entourage as a two-column ledger with groom's side always left, bride's side always right, plus sticky group headers on mobile; Four culturally specific 16px line glyphs for veil, cord, candle, coins/arrhae (and barong/gown for attire); Venue cards ordered south-to-north with a travel-minutes note between them; static SVG route with click-to-load map instead of an iframe; Gallery img onerror -> dataset.empty fallback so missing prenup files never show broken images; Hashtag set with TIN and JOE upright 600 against italic 300; data-lite flag from saveData / deviceMemory to drop the texture filter on cheap phones; The 'Found in the file' defect list (Cormorant+Garant, #meal-group, playlist URL in YOUTUBE_VIDEO_ID, unclosed entourage divs) | weak: The signature is a gate. A 50-something guest opening from Messenger lands on a closed envelope with a 'scroll to open' hint and a 140vh sticky hero; the envelope-opening splash is ALSO the single most common trope in Filipino e-invites, so it costs time without buying uniqueness; animation-timeline: scroll() is not in the Messenger/Facebook in-app WebView on older Android; the fallback only triggers under prefers-reduced-motion or data-lite, so most real guests get the JS flap hinge plus a fixed feTurbulence body::before plus mask-image deckle edges, all on one sticky layer. That is exactly the jank recipe the plan admits it has not tested; A wax seal as the submit control is a novelty affordance; the persona expects a button. 'Seal & send' text helps but 64px round seal still reads as decoration; --foil #B9A27A champagne gold is a new colour outside the mandated palette; brief said purple/blue/sage; Church card in the sketch shows no street address, only 'Bacolod City' and a 'Directions' link; for 'where is the church' the address must be visible without a tap; No Waze, no tel: links in the venue; contact numbers are buried in an 'enclosure strip' under the programme; Palette as a horizontal-scroll fan deck with hex on tap/hover: hover does not exist, and horizontal scroll inside a page is the interaction this persona most often misses; Six SVG files plus a glyph sprite plus a liner tile plus countdown/envelope/reveal/rsvp/music/ui JS: the widest build surface of the three
  garden: total 27 | steal: Vine that becomes structure: the same plant is the programme stem, the entourage branches and the footer wreath (the test 'remove it and the sections lose their skeleton'); Mobile entourage as an indented ceremony-order list where indent = rank, couples on one line with surname once ('Jun & Snooky Alvarez'); Principal sponsors typeset heaviest, parents as roots: encodes the real Filipino hierarchy; Venue as a drawn route north with km/minutes between the three hydrangea pins, no iframe at all; Herbarium specimen labels with 'coll. Bacolod, xi.2026' and a plant per colour family; Dress code split by sponsors vs guests, not women vs men; Gallery tap opens the full image in the same tab (the most reliable behaviour in Messenger's in-app browser); Footer verse as a textPath ring with the plain <p> duplicate for legibility; Wider stroke on mobile (1.5px at 360px) because 1px lines vanish on cheap screens | weak: Countdown spelled out as 'ninety days, four hours and twelve minutes from now' in italic: a 50-something guest reads a number faster than a sentence, and it is the one thing they glance at; Mobile nav is 'J & C' and the word 'Menu'; no persistent RSVP control anywhere. The only RSVP entry is the hero button and then the section itself; Entourage tree on desktop cannot actually connect branches to reflowing names with 'row-level background' SVGs; either the lines are decorative and misaligned or you end up absolutely positioning after all. On mobile, 64px indent at 360px leaves ~250px for 'Rodolfo Perarin & Jenelyn Curameng' at 18px; One green drawing ink for everything demotes the mandated primary purple to a 55% wash; the page will read sage, not purple/blue/sage; Vine path assembled from 13 segment templates over document height with per-section Y values written by script and recomputed on resize: fonts swapping, images loading and the FAQ opening all move section tops; the vine will drift out of register unless you rebuild the path on every mutation; No map at all; 'Open the route in Google Maps' is a single link, and the sketch gives no address for the reception or after-party, only the church; Palette hex-copy on tap is for designers; a tita wants to show a colour to the modista, not paste #C5A8E0; Herbarium sheets plus arches plus a tree plus a wreath plus a textPath: a lot of bespoke SVG drawing for a no-build vanilla site
  guest-journey: total 32 | steal: Bottom thumb bar on mobile: Schedule | Send our RSVP | Menu, hiding while the RSVP section is in view and flipping to 'Replied' via localStorage; RSVP form ergonomics: mobile number before email with a reason ('so we can text you'), 48px inputs, 16px font to stop iOS zoom, autocomplete/inputmode, stepper 'We're coming as [-] 2 [+] (including you)', honeypot; Venue 'tickets' with Navigate / Waze / Add to calendar pills per stop, visible street address, parking note, approx drive minutes; map iframe only on tap; Hero countdown as one big days number with a second line that changes meaning over time ('...and n days to reply' / 'today, 2:00 PM, Queen of Peace'); Swatch card for the dressmaker (Save swatch card -> PNG -> share sheet) instead of a designer's hex strip; Entourage on an ampersand axis (grid 1fr auto 1fr) with sticky group labels and a '(ninong & ninang)' gloss; Share to Messenger (navigator.share -> fb-messenger://share -> copy) on the hashtag; FAQ final item 'How do we reach you?' with tel: and m.me links so contact works even if JS fails; Misregistered 2px wash behind ink lines: the cheapest 'this looks printed' trick, pure CSS/SVG; Byte budget and size-adjust fallback metrics so the font swap does not jump on 4G | weak: Least theatrical of the three; the vine spine is the same trick as 'garden' minus the tree, and the section layouts are mostly well-set lists. Would not be mistaken for a template, but it will not make anyone gasp either; Ampersand axis at 360px gives each half ~150px; 'Mrs. Ma. Cynthia Alvarez Simene' at 20px wraps to three lines and the principal sponsor rows become a ragged zigzag. The plan waves this through; Three font families (Cormorant, Newsreader, Instrument Sans) is ~110KB; two would do and the persona is on mobile data; Two sticky bottom bars (music mini-player above the nav bar) on a phone is a lot of chrome; and the 'Save swatch card' via navigator.share({files}) and the .ics data: URL both degrade unpredictably inside Messenger's in-app browser; Palette h2 'Wear these to the garden' and dress code h2 'Formal or semi-formal, in our colors' are sentences, not headings; a menu with those labels reads odd; 'Load the map' still means a guest who wants the church on a map has to tap twice (load, then navigate); the route sketch is pretty but it is not a map of B.S. Aquino Drive